# ========================================
# CONFIGURACIÓN DEL SERVIDOR
# ========================================
spring.application.name=sistema-gestion-hospitalaria
server.port=${PORT:8089}
server.url=http://localhost:8089

# ========================================
# CONFIGURACIÓN DE BASE DE DATOS
# ========================================
spring.datasource.url=jdbc:postgresql://db.tsqvkkyenytbjbymmpkx.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=kI]0,BKk2Y1b
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=America/Argentina/Buenos_Aires
spring.jpa.open-in-view=false

# ========================================
# CONFIGURACIÓN DE JWT
# ========================================
jwt.secret=${JWT_SECRET:defaultSecretKeyForDevelopmentOnly123456789}
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# ========================================
# CONFIGURACIÓN DE CORS
# ========================================
cors.allowed-origins=${CORS_ORIGINS:http://localhost:3000,http://localhost:5173}

# ========================================
# CONFIGURACIÓN DE LOGGING
# ========================================
logging.level.root=INFO
logging.level.com.hospital=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# ========================================
# CONFIGURACIÓN DE ACTUATOR
# ========================================
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Evitar que la aplicación se cierre
spring.main.keep-alive=true

# Configurar web server
server.shutdown=graceful
spring.lifecycle.timeout-per-shutdown-phase=20s

# Log completo para debug
logging.level.org.springframework.boot=DEBUG
logging.level.org.springframework.web=DEBUG

# Swagger UI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha

# Gmail SMTP para envío de correos
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=santy15021502@gmail.com
spring.mail.password=tzqu vayb kecm euwx
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Async (necesario para @Async)
spring.task.execution.pool.core-size=2
spring.task.execution.pool.max-size=5